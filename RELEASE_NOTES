A new version of Rss is out. It adds RSS extensions Comment API and blogChannel. There are several improvements/changes to BaseExtension that make it easier to add RSS extensions.

For complete list of changes since previous version, see CHANGELOG.md.
